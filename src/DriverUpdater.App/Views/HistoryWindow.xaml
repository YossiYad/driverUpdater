<ui:FluentWindow x:Class="DriverUpdater.App.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:DriverUpdater.App.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:HistoryViewModel}"
        Title="DriverUpdater - History"
        Icon="pack://application:,,,/Assets/app.ico"
        Width="1000" Height="600"
        MinWidth="780" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Loaded="OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="20,12"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Update history" FontSize="18" FontWeight="SemiBold" />
                <Button Margin="20,0,0,0"
                        Padding="14,4"
                        Content="Refresh"
                        Command="{Binding LoadCommand}" />
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1"
                  Margin="20,12,20,8"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  RowHeight="34"
                  VirtualizingPanel.IsVirtualizing="True"
                  ItemsSource="{Binding Operations}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
                <DataGridTextColumn Header="Device" Binding="{Binding DeviceName}" Width="*" MinWidth="240" />
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="120" />
                <DataGridTextColumn Header="From" Binding="{Binding FromVersionText}" Width="110" />
                <DataGridTextColumn Header="To" Binding="{Binding ToVersionText}" Width="110" />
                <DataGridTextColumn Header="Started" Binding="{Binding StartedAtText}" Width="130" />
                <DataGridTemplateColumn Header="Actions" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Padding="10,2"
                                    Content="Rollback"
                                    IsEnabled="{Binding CanRollback}"
                                    Command="{Binding DataContext.RollbackCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2"
                Padding="20,8"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
            <TextBlock Text="{Binding StatusText}" Opacity="0.85" FontSize="12" />
        </Border>
    </Grid>
</ui:FluentWindow>
