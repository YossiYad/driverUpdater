<Window x:Class="DriverUpdater.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DriverUpdater.App.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="DriverUpdater"
        Width="1200" Height="780"
        MinWidth="980" MinHeight="600"
        ui:WindowHelper.UseModernWindowStyle="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="20,14"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="DriverUpdater"
                           FontSize="22"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />
                <Border x:Name="AdminBadge"
                        Margin="14,0,0,0"
                        Padding="10,3"
                        CornerRadius="10"
                        Background="#FF2E7D32"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="AdminBadgeText"
                               Text="Administrator"
                               Foreground="White"
                               FontSize="11"
                               FontWeight="SemiBold" />
                </Border>
                <TextBlock Margin="14,0,0,0"
                           VerticalAlignment="Center"
                           Opacity="0.7"
                           Text="v0.1.0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Padding="20,12"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Scan now"
                        Padding="14,6"
                        Command="{Binding ScanCommand}"
                        Style="{DynamicResource AccentButtonStyle}" />
                <Button Grid.Column="1"
                        Margin="8,0,0,0"
                        Padding="14,6"
                        Content="Cancel"
                        Command="{Binding ScanCancelCommand}"
                        IsEnabled="{Binding IsScanning}" />
                <Button Grid.Column="2"
                        Margin="8,0,0,0"
                        Padding="14,6"
                        Content="Clear"
                        Command="{Binding ClearCommand}" />

                <TextBox Grid.Column="4"
                         Width="240"
                         Margin="0,0,8,0"
                         ui:ControlHelper.PlaceholderText="Search device, provider, hardware ID..."
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=150}" />

                <ComboBox Grid.Column="5"
                          Width="180"
                          ui:ControlHelper.PlaceholderText="All categories"
                          ItemsSource="{Binding AvailableCategories}"
                          SelectedItem="{Binding CategoryFilter}" />
            </Grid>
        </Border>

        <DataGrid Grid.Row="2"
                  Margin="20,12,20,8"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  RowHeight="34"
                  SelectionMode="Extended"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  EnableRowVirtualization="True"
                  ItemsSource="{Binding DriversView}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Device"
                                    Binding="{Binding DeviceName}"
                                    Width="*"
                                    MinWidth="220" />
                <DataGridTextColumn Header="Category"
                                    Binding="{Binding Category}"
                                    Width="120" />
                <DataGridTextColumn Header="Class"
                                    Binding="{Binding DeviceClass}"
                                    Width="120" />
                <DataGridTextColumn Header="Version"
                                    Binding="{Binding CurrentVersionText}"
                                    Width="130" />
                <DataGridTextColumn Header="Date"
                                    Binding="{Binding CurrentDateText}"
                                    Width="110" />
                <DataGridTextColumn Header="Provider"
                                    Binding="{Binding Provider}"
                                    Width="170" />
                <DataGridCheckBoxColumn Header="Signed"
                                        Binding="{Binding IsSigned}"
                                        Width="70" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3"
                Padding="20,10"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusText}"
                           Opacity="0.85"
                           VerticalAlignment="Center"
                           FontSize="12" />

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <ProgressBar Width="120"
                                 Height="6"
                                 Margin="0,0,12,0"
                                 IsIndeterminate="True"
                                 Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <TextBlock Text="{Binding ProgressText}"
                               Opacity="0.7"
                               FontSize="12"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
